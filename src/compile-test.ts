import * as React from 'react';

import { Hello } from '@/components/Paragraph';

const _radsr = require('asdads');

const asd = {
  asddas: 213231231,
  ad: 0 * 1,
};

alert(alert.bind(this));

const use = function use(...args) {};

const use2 = function (a: any = Number(1)) {
  alert;
};
